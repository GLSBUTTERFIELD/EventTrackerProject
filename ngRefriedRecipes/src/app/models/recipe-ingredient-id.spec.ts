import { RecipeIngredientId } from './recipe-ingredient-id';

describe('RecipeIngredientId', () => {
  it('should create an instance', () => {
    expect(new RecipeIngredientId()).toBeTruthy();
  });
});
